[package]
name = "discord-quest-helper"
version = "0.10.4"
edition = "2021"
default-run = "discord-quest-helper"

[lib]
name = "discord_quest_helper_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2.11", features = [] }
tauri-plugin-opener = "=2.5.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
discord-cdp-launch-core = { path = "../crates/discord-cdp-launch-core", features = ["serde"] }
discord-sdk = "0.4.0"
once_cell = "1.21"

tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.13", default-features = false, features = ["native-tls", "json", "charset", "http2", "system-proxy", "socks"] }
regex = "1"
base64 = "0.22"
aes-gcm = "0.10"
rand = "0.10"
anyhow = "1"
arc-swap = "1.7"
ctrlc = "3.5"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
tokio-tungstenite = { version = "0.28", features = ["native-tls"] }
futures-util = "0.3"
flate2 = "1"
url = "2"
tauri-plugin-fs = "=2.4.5"
tauri-plugin-dialog = "=2.6.0"
# Zero-on-drop wrapper for the Authorization token captured by CDP auto-login;
# the raw token is held only in Rust, never serialized to the frontend or logs.
zeroize = "1"
sha2 = "0.10"
libc = "0.2"
# OS credential store for proxy credentials only (Windows Credential Manager,
# macOS Keychain, Linux Secret Service via zbus). Same identifier as the app's
# bundle id with a `.proxy` suffix. Secrets never touch JSON or localStorage.
keyring = "4"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_Globalization",
    "Win32_Storage_FileSystem",
    "Win32_System_LibraryLoader",
    "Win32_UI_HiDpi",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
    "Storage_Streams",
] }
windows-registry = "0.6"

[target.'cfg(target_os = "linux")'.dependencies]
# Precise SIGTERM/SIGKILL for the simulated-game runner: Linux tracks exact PIDs
# (never process names) and verifies /proc/<pid>/exe before signalling, so a
# recycled PID or a real game with the same name is never killed.
nix = { version = "0.30", features = ["signal", "process"] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
